# Kopyala: cp .env.example .env  — sonra gerçek hash'i yaz.
# Hash üretme: docker compose run --rm api hash-password '<parola>'
# Not: .env içindeki $ işaretleri compose tarafından olduğu gibi okunur, kaçış gerekmez.
PASSWORD_HASH=$2a$12$ornek-hash-buraya
